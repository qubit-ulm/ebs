language: cpp

compiler:
  - clang
  - gcc

before_install:
  - sudo add-apt-repository --yes ppa:andykimpe/cmake
  - sudo apt-get update -qq
  - sudo apt-get install -qq cmake 

before_script:
   - mkdir build
   - cd build
   - cmake ..

script: make
